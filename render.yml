services:
  - type: web
    name: whatsapp-chatbot
    env: docker
    plan: free
    branch: main

    dockerfilePath: ./Dockerfile

    autoDeploy: true

    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m
