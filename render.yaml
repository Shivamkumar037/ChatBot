services:
  - type: web
    name: whatsapp-chatbot
    env: java
    plan: free
    branch: main

    buildCommand: ./mvnw clean install

    startCommand: java -jar target/*.jar

    autoDeploy: true

    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m